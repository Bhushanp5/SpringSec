import { FormatnMePipe } from './formatn-me.pipe';

describe('FormatnMePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatnMePipe();
    expect(pipe).toBeTruthy();
  });
});
